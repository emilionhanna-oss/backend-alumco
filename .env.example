# Puerto del servidor (default: 3000)
PORT=3000

# CORS: permite el frontend local. Puedes agregar más orígenes separando por coma.
CORS_ORIGIN=http://localhost:5173

# JWT: clave para firmar tokens (NO usar un valor débil en producción)
JWT_SECRET=change_me_in_local_env
